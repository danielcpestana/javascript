// Valor por referência
//
 const nomes = ['Daniel', 'Luiz', 'Joana', 'João', 'Gabriel']

 //pop
//  nomes.splice(-1, 1)
// shift
// nomes.splice(0, 1)
//push
// nomes.splice(nomes.length, 0, 'Abel')
//unshift
// nomes.splice(0, 0, 'Charles')
console.log(nomes)